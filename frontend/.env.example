# Copy this to .env.local for local dev
# For production (Render), set VITE_API_URL in the Render dashboard environment variables

# Local development
VITE_API_URL=http://127.0.0.1:8000

# Production example (replace with your actual Render backend URL)
# VITE_API_URL=https://smartflash-backend.onrender.com
